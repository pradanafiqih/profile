import type { MediaItem } from "./schema";

/*
 * Item media (gambar + atribusi). Diisi pada Task 2.5.
 * Setiap item wajib `credit`, `license`, `sourceUrl` (R1.5, R5.2).
 */
export const media: MediaItem[] = [];
